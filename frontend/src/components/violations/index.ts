// Violations feature components
// The violations UI is self-contained in src/pages/violations.tsx.
// Re-export the hook here so feature-level imports work cleanly.
export { useViolations } from '@/hooks/useViolations';
