// Chat feature components
// The chat UI is self-contained in src/pages/chat.tsx using the useChat hook.
// Re-export the hook here so feature-level imports work cleanly.
export { useChat } from '@/hooks/useChat';
