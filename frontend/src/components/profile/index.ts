// Profile / analytics feature components
// The profile UI is self-contained in src/pages/profile.tsx.
// Re-export the hook here so feature-level imports work cleanly.
export { useProfile } from '@/hooks/useProfile';
